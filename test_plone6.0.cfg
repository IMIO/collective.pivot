[buildout]

parts +=
    instance

extends =
    https://dist.plone.org/release/6.0-latest/versions.cfg
    base.cfg

[instance]
zodb-temporary-storage = off
eggs -=
    ipdb
    iw.debug

zcml -= iw.debug

[versions]
setuptools = 65.5.1
zc.buildout = 3.0.1
